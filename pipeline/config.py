"""Danh sách symbol theo dõi.

Để mặc định trong code cho đơn giản (side-project, không cần UI quản lý);
nếu sau này cần đổi danh sách mà không redeploy, chuyển sang Airflow Variable.
"""

VN_SYMBOLS = ["VNM", "FPT", "HPG", "VCB", "VIC"]
INTL_SYMBOLS = ["AAPL", "MSFT", "GOOGL", "NVDA", "AMZN"]
